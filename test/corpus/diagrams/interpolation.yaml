id: diagrams.3
source: diagrams
tags: [exists, tope-prop, leq, draft]
provenance:
  kind: speculative
  note: >-
    a point between two comparable points, as needed to place an intermediate
    vertex when subdividing an arrow. Witnessed by either endpoint, so the
    judgement is derivable but the useful witness is not the one a naive
    procedure would return. A good test of what we want to get out of the
    solver beyond yes/no.
context:
  - "s : 2"
  - "t : 2"
premises:
  - 's <= t'
goal: 'exists (r : 2), s <= r /\ r <= t'
expected:
  entailment: derivable
  premises_consistency: consistent
