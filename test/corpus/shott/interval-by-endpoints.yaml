id: shott.interval-by-endpoints
kind: rzk
source: shott
tags: [tope-prop, eq, coverage, classical-separator]
provenance:
  kind: handwritten
  note: "the sharpest test in this group. A solver that treats the interval as two truth values accepts this file, and then an arrow in a Segal type is determined by its endpoints, which collapses the theory. Compare shott.square-by-linearity, which must still be accepted."
input:
  rzk: interval-by-endpoints.rzk
expected:
  typechecks: rejected
  error_contains: tope
